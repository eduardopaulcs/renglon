# Normalize line endings: the repo stores LF, the working copy uses the native ones.
# Without this, every file shows up as modified after cloning on Windows.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.keystore binary
*.jks binary
